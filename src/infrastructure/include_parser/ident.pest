WHITESPACE = _{ " " | "\t" | "\r" | "\n" }

// ascii char
ascii_char = { '\x00'..'\x7f' }

alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }

ident = { (alpha | digit)+ }

sharp = _{ "#" }

imports = { sharp ~ "include" ~ "<" ~ (alpha)+ ~ "." ~ "h" ~ ">"}
